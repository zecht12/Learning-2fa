const nextConfig = {
    images: {
        domains: [ 
            'avatars.githubusercontent.com',
            'lh3.googleusercontent.com',
            'localhost', // Add localhost as a domain
        ]
    }
};

module.exports = nextConfig;
